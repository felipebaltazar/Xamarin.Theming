<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Xamarin.Theming.Views.MainPage"
             BackgroundColor="{DynamicResource CorDeFundo}">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="20*"/>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="5*"/>
                <RowDefinition Height="25*"/>
                <RowDefinition Height="40*"/>
            </Grid.RowDefinitions>
            
            <Label Grid.Row="1" Text="{DynamicResource TituloString}" Style="{StaticResource LabelEstiloTitulo}" />
            <Label Grid.Row="2" Text="{DynamicResource CorpoString}" Style="{StaticResource LabelEstiloCorpo}" />
            <Image Grid.Row="3" Style="{DynamicResource EstiloLogoXamarin}"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>